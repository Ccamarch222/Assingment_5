//Create a function calculateArea that takes the radius of a circle as an input
/*and returns the area of the circle.*/
function calculateArea(r:number) {
    return(3.14)*r**2
}
const area=calculateArea(5);
console.log(area);